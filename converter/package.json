{
  "name": "sign-visualizations_converter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "macos": "npx ts-node -T convert.ts --type macos",
    "linux": "npx ts-node -T convert.ts --type linux",
    "windows": "npx ts-node -T convert.ts --type windows"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.1",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "ts-node": "^10.4.0"
  }
}
